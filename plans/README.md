# Implementation Plans

Generated by the `improve` skill (quick audit) on 2026-06-12. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Enable strict TypeScript mode in tsconfig | P1 | S | — | DONE |
| 002  | Fix App.tsx lint errors | P1 | S | — | DONE |
| 003  | Fix API error handling for non-HTTP error responses | P1 | S | — | DONE |
| 004  | Deduplicate gold calculation functions into shared module | P2 | S | — | DONE |
| 005  | Add AbortController to API fetch for safe unmount and deduplication | P2 | S | 003 | DONE |
| 006  | Fix EUR spot price calculation — multiply instead of divide | P1 | S | — | DONE |
| 007  | Validate asset inputs and guard division by zero | P1 | S | — | DONE |
| 008  | Add React Error Boundary | P2 | S | — | DONE |
| 009  | Guard localStorage setItem against QuotaExceededError | P2 | S | — | DONE |
| 010  | Remove dead code (4 unused exports) | P3 | S | — | DONE |
| 011  | Move calculateSpotEurPerOz from api.ts to calculations.ts | P3 | S | 006 | DONE |
| 012  | Deduplicate P&L calculation into calculations.ts | P3 | S | 011 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **005** depends on **003** because both modify `src/lib/api.ts`. Execute 003 first to avoid merge conflicts.
- **011** depends on **006** because 006 renames `eurUsd` to `eurPerUsd` and fixes the operator in `calculateSpotEurPerOz`. The move in 011 must pick up the corrected version.
- **012** depends on **011** because 012 adds new functions to `src/lib/calculations.ts` — best to do that after 011 has established the function in its new home.

## Recommended execution order

1. **006** (fix price calc — CRITICAL bug, all valuations wrong)
2. **007** (validate inputs — HIGH severity, prevents bad data + Infinity display)
3. **008** (Error Boundary — architectural resilience)
4. **009** (setItem guard — prevents crashes on storage quota)
5. **010** (dead code cleanup — independent, quick win)
6. **011** (move calculation — depends on 006)
7. **012** (deduplicate P&L — depends on 011)

Plans 006 and 007 are independent and can execute in parallel (they modify disjoint files). Plans 008, 009, and 010 are also independent of each other and of 006/007.

## Findings considered and rejected

From prior session:
- **No test infrastructure**: deliberately deferred by the user ("I'll add tests later"). Not planned.
- **No dark mode toggle UI**: direction finding, not a defect. Not planned for this round.
- **No asset editing capability**: direction finding, not a defect. Not planned for this round.
- **No data export/import**: direction finding, not a defect. Not planned for this round.

From this session:
- **API key in URL query param**: by-design per metalpriceapi.com's protocol. Cannot use Authorization header.
- **Stale closure in useEffect**: intentional eslint-disable-next-line suppression, standard React pattern. Risk negligible.
- **Race condition in refreshPrice finally block**: edge case with very low practical probability.
- **Array .push() mutation in storage.ts**: safe because JSON.parse returns a fresh array each call.
- **TypeScript ~6.0.2 (pre-release)**: appears intentional — project uses erasableSyntaxOnly TS 6 feature.